[project]
name = "github-notification"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
github-notification = "github_notification:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["github_notification"]
